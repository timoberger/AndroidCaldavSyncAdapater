<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.gege.caldavsyncadapter"
    android:versionCode="16"
    android:versionName="1.8.1-beta.1" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name=".syncadapter.SyncService" android:exported="false">
            <intent-filter>
                <action android:name="android.content.SyncAdapter" />
            </intent-filter>

            <meta-data
                android:name="android.content.SyncAdapter"
                android:resource="@xml/syncadapter" />
        </service>
        <service
            android:name=".authenticator.AuthenticationService" android:exported="false">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <activity
            android:name=".authenticator.AuthenticatorActivity"
            android:label="@string/title_activity_authenticator"
            android:windowSoftInputMode="adjustResize|stateVisible" 
            android:theme="@android:style/Theme.Black" >
        </activity>
        
		<activity
            android:name=".activities.SettingsActivity"
            android:theme="@android:style/Theme.Black" >
			<intent-filter>
				<action android:name="org.gege.caldavsyncadapter.settings.MAIN"/>
				<category android:name="android.intent.category.DEFAULT"/>
			</intent-filter>
        </activity>
        <activity
            android:name=".activities.StatsActivity"
            android:theme="@android:style/Theme.Black" >
			<intent-filter>
				<action android:name="org.gege.caldavsyncadapter.settings.STATS"/>
				<category android:name="android.intent.category.DEFAULT"/>
			</intent-filter>
        </activity>
         <activity
            android:name=".activities.CalendarlistActivity"
            android:theme="@android:style/Theme.Black" >
			<intent-filter>
				<action android:name="org.gege.caldavsyncadapter.settings.CALENDARS"/>
				<category android:name="android.intent.category.DEFAULT"/>
			</intent-filter>
        </activity>
    </application>

</manifest>